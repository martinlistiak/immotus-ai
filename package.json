{
  "name": "immotus-ai",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "yarn workspaces foreach -A -pvi -j unlimited run dev",
    "build": "yarn workspaces foreach -A -pvi -j unlimited run build",
    "start:prod": "yarn workspaces foreach -A -pvi -j unlimited run start:prod"
  },
  "packageManager": "yarn@4.9.1",
  "dependencies": {
    "react-dnd": "^16.0.1",
    "react-dnd-html5-backend": "^16.0.1"
  }
}
